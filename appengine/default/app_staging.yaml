runtime: go113
env: standard
instance_class: F1

automatic_scaling:
  min_idle_instances: 0
  max_idle_instances: 1
  min_instances: 0
  max_instances: 1
  max_concurrent_requests: 80

service: default

env_variables:
  DEPLOY: "staging"
